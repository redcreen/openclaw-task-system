{
  "id": "openclaw-task-system",
  "uiHints": {
    "enabled": {
      "label": "Enabled",
      "help": "Enable the OpenClaw Task System plugin."
    },
    "taskMessagePrefix": {
      "label": "Message Prefix",
      "help": "Prefix added to task-system visible status messages such as receipts and watchdog updates."
    },
    "pythonBin": {
      "label": "Python Binary",
      "help": "Python executable used to invoke the task system runtime hooks."
    },
    "runtimeRoot": {
      "label": "Runtime Root",
      "help": "Absolute path to the openclaw-task-system project root."
    },
    "configPath": {
      "label": "Config Path",
      "help": "Optional path to task_system.json used by the runtime hooks."
    },
    "debugLogPath": {
      "label": "Debug Log Path",
      "help": "Optional file path for plugin hook trace logs during integration debugging."
    },
    "registerOnBeforeDispatch": {
      "label": "Register On Dispatch",
      "help": "Register long tasks during before_dispatch."
    },
    "sendImmediateAckOnRegister": {
      "label": "Immediate Ack On Register",
      "help": "Immediately send a short acknowledgement when a new long task is first registered."
    },
    "sendImmediateAckForShortTasks": {
      "label": "Immediate Ack For Short Tasks",
      "help": "Also send the same lightweight acknowledgement for requests that are not promoted to long tasks."
    },
    "immediateAckTemplate": {
      "label": "Immediate Ack Template",
      "help": "Short visible acknowledgement sent right after first long-task registration."
    },
    "shortTaskFollowupTimeoutMs": {
      "label": "Short Task Followup Timeout",
      "help": "How long to wait before sending a delayed 'still processing' note for slow short tasks."
    },
    "shortTaskFollowupTemplate": {
      "label": "Short Task Followup Template",
      "help": "Delayed status message for short tasks that still have not returned within the configured timeout."
    },
    "syncProgressOnMessageSending": {
      "label": "Sync Progress On Send",
      "help": "Touch active task progress whenever OpenClaw is about to send a visible message."
    },
    "finalizeOnAgentEnd": {
      "label": "Finalize On Agent End",
      "help": "Automatically complete or fail the active task when the agent run ends."
    },
    "minProgressMessageLength": {
      "label": "Min Progress Length",
      "help": "Minimum outbound message length before it is treated as a visible progress update."
    },
    "ignoreProgressPatterns": {
      "label": "Ignore Progress Patterns",
      "help": "Regex patterns for outbound messages that should not count as progress."
    },
    "enableHostFeishuDelivery": {
      "label": "Enable Feishu Host Delivery",
      "help": "Process Feishu send-instructions inside the plugin with OpenClaw host runtime instead of CLI message send."
    },
    "hostDeliveryPollMs": {
      "label": "Host Delivery Poll MS",
      "help": "Polling interval for the plugin-side Feishu delivery bridge."
    },
    "enableContinuationRunner": {
      "label": "Enable Continuation Runner",
      "help": "Poll for scheduled delayed-reply continuations and execute them when due."
    },
    "continuationPollMs": {
      "label": "Continuation Poll MS",
      "help": "Polling interval for scheduled continuation tasks such as delayed replies."
    }
  },
  "configSchema": {
    "type": "object",
    "additionalProperties": false,
    "properties": {
      "enabled": { "type": "boolean" },
      "taskMessagePrefix": { "type": "string" },
      "pythonBin": { "type": "string" },
      "runtimeRoot": { "type": "string" },
      "configPath": { "type": "string" },
      "debugLogPath": { "type": "string" },
      "defaultAgentId": { "type": "string" },
      "registerOnBeforeDispatch": { "type": "boolean" },
      "sendImmediateAckOnRegister": { "type": "boolean" },
      "sendImmediateAckForShortTasks": { "type": "boolean" },
      "immediateAckTemplate": { "type": "string" },
      "shortTaskFollowupTimeoutMs": { "type": "integer", "minimum": 1000 },
      "shortTaskFollowupTemplate": { "type": "string" },
      "syncProgressOnMessageSending": { "type": "boolean" },
      "finalizeOnAgentEnd": { "type": "boolean" },
      "minProgressMessageLength": { "type": "integer", "minimum": 1 },
      "enableHostFeishuDelivery": { "type": "boolean" },
      "hostDeliveryPollMs": { "type": "integer", "minimum": 1000 },
      "enableContinuationRunner": { "type": "boolean" },
      "continuationPollMs": { "type": "integer", "minimum": 1000 },
      "ignoreProgressPatterns": {
        "type": "array",
        "items": { "type": "string" }
      }
    }
  }
}
